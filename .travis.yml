language: node_js
node_js:
  - 8

cache:
  directories:
  - node_modules

script:
  - npm run test:coverage
  - npm run build

after_success:
  - ./node_modules/codecov/bin/codecov

deploy:
  provider: npm
  skip_cleanup: true
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
  on:
    branch: master
    condition: $TRAVIS_PULL_REQUEST = false

branches:
  only:
    - master
